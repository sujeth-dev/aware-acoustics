{
  "name": "aware-acoustics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate": "node scripts/generate-pages.js",
    "build": "vite build",
    "build:production": "AWARE_ENV=production npm run validate:data && AWARE_ENV=production vite build",
    "preview": "vite preview",
    "validate:data": "node scripts/validate-data.js",
    "test:smoke": "node tests/render-record.smoke.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
